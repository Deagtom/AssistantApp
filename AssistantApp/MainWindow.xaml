<Window x:Class="AssistantApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:AssistantApp.Converters"
        Title="Symptom Assistant" Height="550" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Обучить" Command="{Binding TrainModelCommand}" Margin="0,0,10,0"/>
            <Button Content="Предсказать" Command="{Binding PredictCommand}" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Command="{Binding SaveUsageCommand}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <GroupBox Header="Симптомы" Grid.Column="0" Margin="0,0,10,0">
                <ScrollViewer>
                    <TreeView ItemsSource="{Binding Categories}">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Symptoms}">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Name}">
                                            <CheckBox.IsChecked>
                                                <MultiBinding Converter="{StaticResource SymptomSelectionMultiConverter}" Mode="OneWay">
                                                    <Binding Path="DataContext.SelectedSymptoms" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                                    <Binding/>
                                                </MultiBinding>
                                            </CheckBox.IsChecked>
                                        </CheckBox>
                                    </DataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </ScrollViewer>
            </GroupBox>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Диагноз:" Margin="0,0,0,5" FontWeight="Bold"/>
                <ComboBox ItemsSource="{Binding Diagnoses}"
                  SelectedItem="{Binding SelectedDiagnosis}"
                  DisplayMemberPath="Name"
                  Margin="0,0,0,20"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
